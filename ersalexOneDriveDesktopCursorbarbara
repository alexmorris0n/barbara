[1mdiff --git a/barbara/barbara_agent.py b/barbara/barbara_agent.py[m
[1mindex 5d1b83e..ef67691 100644[m
[1m--- a/barbara/barbara_agent.py[m
[1m+++ b/barbara/barbara_agent.py[m
[36m@@ -453,10 +453,6 @@[m [mWhen booking, offer the next available slot first. If they need a different time[m
         # Configure AI models from database[m
         # Per Section 3.21 (AI Parameters)[m
         # All LLM params (temperature, top_p, frequency_penalty, presence_penalty) loaded from agent_params table[m
[31m-        # Build debug webhook URL from callback path[m
[31m-        # Per SDK Section 23689: debug_webhook_level 2 sends full transcripts and tool calls[m
[31m-        debug_webhook_url = self.get_full_url("/debug-log") if callback_path else None[m
[31m-        [m
         self.set_params({[m
             "ai_model": models.get("llm_model", "gpt-4.1-mini"),[m
             "openai_asr_engine": models.get("stt_model", "deepgram:nova-3"),[m
[36m@@ -473,9 +469,8 @@[m [mWhen booking, offer the next available slot first. If they need a different time[m
             "conversation_id": phone,[m
             "conscience": "Remember to stay in character as Barbara, a warm and friendly reverse mortgage specialist. Always use the calculate_reverse_mortgage function for any financial calculations - never estimate or guess numbers.",[m
             "local_tz": "America/Los_Angeles",[m
[31m-            # Debug webhook for capturing full transcripts and tool calls[m
[31m-            "debug_webhook_url": debug_webhook_url,[m
[31m-            "debug_webhook_level": 2,  # Level 2 = full verbosity (transcripts, tool calls, etc.)[m
[32m+[m[32m            # NOTE: debug_webhook_url removed - was causing SWML generation to fail[m
[32m+[m[32m            # TODO: Implement properly by building URL manually (get_full_url doesn't take path param)[m
         })[m
         [m
         # Configure voice[m
